@model CategoryListViewModel


<ul class="list-group">
    @foreach (var item in Model.Categories)
    {
        <a asp-controller="Shop"
           asp-action="List"
           asp-route-category="@item.Name.ToLower()"
           class="list-group-item list-group-item-action @(Model.SelectedCategory==item.Name.ToLower()?"active":"" )">
            @* Active yapma işlemi çalışmadı *@
            @item.Name
        </a>
    }
</ul>

@using (Html.BeginForm("List", "Shop", FormMethod.Post))
{


    <div class="product-sidebar">
        <div class="product-sidebar-widget">
            <h4 class="mb-0">E-Commerce Filter</h4>
        </div>
        <div class="product-sidebar-widget">
            <h4 class="product-sidebar-widget-title">Category</h4>
            @foreach (var item in Model.Categories)
            {

                <div class="custom-control custom-checkbox">
                    <input type="checkbox" class="custom-control-input" name="@item.Name" value="@item.Name" id="@item.Name">
                    <label asp-controller="Shop" action class="custom-control-label" for="@item.Name">@item.Name</label>
                </div>
                @*<div class="custom-control custom-checkbox">
                <input type="checkbox" class="custom-control-input" id="cat-2">
                <label class="custom-control-label" for="cat-2">Bilgisayar</label>
            </div>
            <div class="custom-control custom-checkbox">
                <input type="checkbox" class="custom-control-input" id="cat-3">
                <label class="custom-control-label" for="cat-3">Beyaz Eşya</label>
            </div>
            <div class="custom-control custom-checkbox">
                <input type="checkbox" class="custom-control-input" id="cat-4">
                <label class="custom-control-label" for="cat-4">Elektronik</label>
            </div>
            <div class="custom-control custom-checkbox">
                <input type="checkbox" class="custom-control-input" id="cat-5">
                <label class="custom-control-label" for="cat-5">Aksesuar</label>
            </div>*@
                <br />


            }
            <div class="custom-control ml-4">
                <button type="submit" class="btn btn-rounded btn-primary">Listele</button>
            </div>
        </div>

    </div>

   
}


